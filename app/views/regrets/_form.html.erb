<form>
  <% form_for(regret) do |f| %>
    <% if regret.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(regret.errors.count, "error") %> prohibited this regret from being saved:</h2>
        <ul>
          <% regret.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

      <%= f.text_area :message, placeholder: "Send your wishes for the wedding day to the bride and groom" %>
      <%= f.submit "Send your Regrets", class: "btn btn-default" %>
  <% end %>
</form>



